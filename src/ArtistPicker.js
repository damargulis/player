import React from 'react';

export default class ArtistPicker extends React.Component {
  // reuse the grid from album view?
  // source pictures from wikipedia, rotate album covers as backup
  render() {
    return (
      <div>Pick by artist</div>
    )
  }
}
